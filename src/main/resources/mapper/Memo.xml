<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bunge.memo.mapper.MemoMapper">

    <insert id="addMemo">
        <selectKey resultType="int" order="BEFORE" keyProperty="no">
            select coalesce(max(no), 0)+1 from memo
        </selectKey>
        insert into memo
        (no, id, ispublic, readpages, title, keyword, content, created, lastmodified)
        values
        (#{no}, #{id}, #{ispublic}, #{readpages}, #{title}, #{keyword}, #{content}, now(), now())
    </insert>

</mapper>