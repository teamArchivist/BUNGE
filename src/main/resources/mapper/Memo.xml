<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bunge.memo.mapper.MemoMapper">

    <update id="updateReadPage">
        update readstate
        set readpage = readpage + #{readpage}
        where isbn13 = #{isbn13}
          and id = #{id}
    </update>

    <insert id="addMemo">
        <selectKey resultType="int" order="BEFORE" keyProperty="remainpage">
            select (totalpage-readpage) remainpage
            from readstate
            where isbn13 = #{isbn13}
            and id = #{id};
        </selectKey>
            insert into memo
            (id, readpage, remainpage, ispublic, isbn13, cover, title, content, created)
            values
            (#{id}, #{readpage}, #{remainpage}, #{ispublic}, #{isbn13}, #{cover}, #{title}, #{content}, now())
    </insert>


    <select id="getMyMemoList" resultType="com.bunge.memo.domain.Memo">
        select *
        from memo
        where id = #{loginId}
    </select>



</mapper>
