<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bunge.inquiry.mapper.InquiryMapper">

    <!-- 문의글 삽입 -->
    <insert id="insertInquiry">
        INSERT INTO inquiry (member_id, type_id, title, content)
        VALUES (#{memberId}, #{typeId}, #{title}, #{content})
    </insert>

    <!-- 문의글 수정 -->
    <update id="updateInquiry">
        UPDATE inquiry
        SET title = #{title}, content = #{content}, type_id = #{typeId},
            updated_at = CURRENT_TIMESTAMP
        WHERE inquiry_id = #{inquiryId}
    </update>

    <!-- 문의글 삭제 -->
    <delete id="deleteInquiry">
        DELETE FROM inquiry WHERE inquiry_id = #{inquiryId}
    </delete>

    <!-- 문의글 조회 -->
    <select id="selectInquiry" resultType="com.example.model.Inquiry">
        SELECT
            inquiry_id AS inquiryId,
            member_id AS memberId,
            type_id AS typeId,
            title,
            content,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM inquiry
        WHERE inquiry_id = #{inquiryId}
    </select>

    <!-- 문의글 목록 조회 (유형 필터) -->
    <select id="selectInquiriesByType" resultType="com.example.model.Inquiry">
        SELECT
            inquiry_id AS inquiryId,
            member_id AS memberId,
            type_id AS typeId,
            title,
            content,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM inquiry
        WHERE type_id = #{typeId}
    </select>


</mapper>
